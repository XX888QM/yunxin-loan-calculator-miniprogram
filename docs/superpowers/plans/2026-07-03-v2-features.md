# 车贷房贷计算器 V2 功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-07-03-v2-features-design.md` 实现 8 项功能：含费真利率、总利息反推、车价首付模式、尾款贷、以租代购（测算+定价）、提前还款违约金、万/元单位切换、还款日历，外加 `toNumber('')` 兜底修复。

**Architecture:** 计算引擎 `utils/loan.js`（ES5、纯函数、只收"元"）先行扩展并保证向后兼容（新参数缺省时行为与 V1 完全一致）；页面 `pages/index/`（原生小程序，无依赖）负责单位换算与表单派生值；测试为 node 原生 assert 脚本，`npm test` = `tests/loan.test.js` + `tests/page.test.js`。

**Tech Stack:** 微信小程序原生（WXML/WXSS/JS），Node.js assert 测试，无第三方依赖。

## Global Constraints

- 引擎 `utils/loan.js` 用 ES5 风格（`var`、`function`），页面 `pages/index/index.js` 用 ES6（`const`、箭头函数）——与现状一致。
- 引擎所有金额参数一律为"元"；万元换算只发生在页面层。
- 所有引擎函数的新参数必须可缺省，缺省时输出与 V1 完全一致（V1 的既有测试断言一条不许改）。
- 复制文案规则：**租购"定价反推"的复制文本不得出现"目标收益/目标年化/收益率"任何字样**。
- WXML 不得出现"厘"字（page.test.js 有断言）。
- UI 只复用现有类：`field/label/input/unit/quick-row/segmented(.two)/result-grid/metric(.main)`；本计划仅新增一个 `.negative` 颜色类。
- 每个任务：先写失败测试 → 跑确认失败 → 最小实现 → 跑确认通过 → 提交。
- git 提交信息末尾加：`Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh`

---

### Task 0: git 仓库初始化

**Files:**
- Create: `.gitignore`

**Interfaces:**
- Produces: git 仓库基线，后续任务逐个提交。

- [ ] **Step 1: 初始化并做基线提交**

```bash
cd /Users/yunxin/Desktop/开发/车贷房贷计算器
git init
printf 'node_modules/\n.DS_Store\n' > .gitignore
git add -A
git commit -m "chore: V1 基线（6 工具 + 引擎 + 测试）

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

- [ ] **Step 2: 验证**

Run: `git log --oneline`
Expected: 一条基线提交；`npm test` 全绿。

---

### Task 1: toNumber 空串返回 fallback

**Files:**
- Modify: `utils/loan.js:4-9`（toNumber）
- Test: `tests/loan.test.js`

**Interfaces:**
- Produces: `toNumber(value, fallback)` — 空串/纯空白/纯逗号输入返回 `fallback`（缺省 0）。所有后续任务依赖此语义。
- 已知行为变化：`pages/index/index.js` buildComboResult 中 `toNumber(form.years, 30)` 空年限从 1 个月变为 30 年（这正是修复目的，页面显示仍为全 0，无可见回归）。

- [ ] **Step 1: 写失败测试** — 在 `tests/loan.test.js` 的 `console.log` 行之前追加：

```js
assert.strictEqual(loan.toNumber('', 30), 30)
assert.strictEqual(loan.toNumber('   ', 7), 7)
assert.strictEqual(loan.toNumber(',', 5), 5)
assert.strictEqual(loan.toNumber(''), 0)
assert.strictEqual(loan.toNumber('1,234.5'), 1234.5)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/loan.test.js`
Expected: AssertionError（`toNumber('', 30)` 当前返回 0）。

- [ ] **Step 3: 最小实现** — 替换 `utils/loan.js` 的 toNumber：

```js
function toNumber(value, fallback) {
  if (fallback === undefined) fallback = 0
  if (value === null || value === undefined) return fallback
  var s = String(value).replace(/,/g, '').trim()
  if (s === '') return fallback
  var n = Number(s)
  return Number.isFinite(n) ? n : fallback
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test`
Expected: `loan calculator checks passed` + `page checks passed`。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "fix: toNumber 空串/空白输入返回 fallback

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 2: inferMonthlyRateFromPayment 支持尾款

**Files:**
- Modify: `utils/loan.js:145-176`（presentValueOfPayment、inferMonthlyRateFromPayment）
- Test: `tests/loan.test.js`

**Interfaces:**
- Produces: `inferMonthlyRateFromPayment(principal, monthlyPayment, months, balloon)` — 第 4 参 `balloon`（元，缺省 0）：现值方程为 Σ pay/(1+r)^i + balloon/(1+r)^n = principal。balloon=0 时与 V1 完全一致。`pay×n + balloon ≤ principal` 时返回 0。
- Consumes: Task 1 的 toNumber（经 nonNegative）。

- [ ] **Step 1: 写失败测试** — `tests/loan.test.js` 追加：

```js
// balloon=0 与旧行为一致
closeTo(loan.inferMonthlyRateFromPayment(350000, 11816.5, 36, 0), 0.01095, 0.000001)
// 尾款往返：闭式月供反推应还原利率
;(function () {
  const P = 200000, B = 80000, m = 36, r = 0.012
  const pay = (P - B / Math.pow(1 + r, m)) * r / (1 - Math.pow(1 + r, -m))
  closeTo(loan.inferMonthlyRateFromPayment(P, pay, m, B), r, 0.0000001)
})()
// 月供×期数+尾款 恰好等于本金 → 零利率
assert.strictEqual(loan.inferMonthlyRateFromPayment(120000, 3000, 12, 84000), 0)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/loan.test.js`
Expected: 第二条 closeTo 失败（当前第 4 参被忽略）。

- [ ] **Step 3: 实现** — 替换两个函数：

```js
function presentValueOfPayment(monthlyPayment, months, monthlyRate, balloon) {
  var pv = 0
  for (var i = 1; i <= months; i += 1) {
    pv += monthlyPayment / Math.pow(1 + monthlyRate, i)
  }
  return pv + (balloon || 0) / Math.pow(1 + monthlyRate, months)
}

function inferMonthlyRateFromPayment(principal, monthlyPayment, months, balloon) {
  principal = nonNegative(principal)
  monthlyPayment = nonNegative(monthlyPayment)
  balloon = nonNegative(balloon)
  months = normalizeMonths(months)
  if (principal <= 0 || monthlyPayment <= 0) return 0
  if (monthlyPayment * months + balloon <= principal) return 0

  var low = 0
  var high = 0.02
  while (presentValueOfPayment(monthlyPayment, months, high, balloon) > principal && high < 1) {
    high *= 2
  }

  for (var i = 0; i < 80; i += 1) {
    var mid = (low + high) / 2
    if (presentValueOfPayment(monthlyPayment, months, mid, balloon) > principal) {
      low = mid
    } else {
      high = mid
    }
  }

  return (low + high) / 2
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test`
Expected: 全绿（V1 断言不变——旧签名 3 参调用 balloon=undefined→nonNegative→0）。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 利率反推支持末期尾款现金流

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 3: calcBalloonLoan 尾款贷

**Files:**
- Modify: `utils/loan.js`（新函数插在 calcAffordableLoan 之后；module.exports 追加）
- Test: `tests/loan.test.js`

**Interfaces:**
- Consumes: Task 2 的 inferMonthlyRateFromPayment（仅测试用）；nonNegative/normalizeMonths/monthlyRateFromAnnual/isZeroRate/baseResult。
- Produces: `calcBalloonLoan(principal, annualRatePercent, months, balloonAmount)` → baseResult 结构（method='balloon'）+ `balloonAmount` 字段；`monthlyPayment` 为常规月供，`lastPayment` = 月供+尾款；尾款自动夹到 `[0, principal]`。

- [ ] **Step 1: 写失败测试** — 追加：

```js
;(function () {
  const P = 200000, B = 80000, m = 36, r = 0.012
  const res = loan.calcBalloonLoan(P, 14.4, m, B)
  const pay = (P - B / Math.pow(1 + r, m)) * r / (1 - Math.pow(1 + r, -m))
  closeTo(res.monthlyPayment, pay, 0.001)
  closeTo(res.lastPayment, pay + B, 0.01)
  closeTo(res.totalInterest, pay * m + B - P, 0.01)
  closeTo(res.schedule[m - 1].balance, 0, 0.000001)
  assert.strictEqual(res.balloonAmount, B)
})()
// 尾款为0 退化成等额本息
closeTo(loan.calcBalloonLoan(350000, 13.14, 36, 0).monthlyPayment,
  loan.calcEqualInstallment(350000, 13.14, 36).monthlyPayment, 0.000001)
// 零利率
closeTo(loan.calcBalloonLoan(120000, 0, 12, 60000).monthlyPayment, 5000, 0.000001)
closeTo(loan.calcBalloonLoan(120000, 0, 12, 60000).lastPayment, 65000, 0.000001)
// 尾款超本金 → 夹到本金，月供=纯利息
closeTo(loan.calcBalloonLoan(100000, 12, 12, 999999).monthlyPayment, 1000, 0.01)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/loan.test.js`
Expected: `loan.calcBalloonLoan is not a function`。

- [ ] **Step 3: 实现** — 插入函数并在 module.exports 加 `calcBalloonLoan: calcBalloonLoan,`：

```js
function calcBalloonLoan(principal, annualRatePercent, months, balloonAmount) {
  principal = nonNegative(principal)
  months = normalizeMonths(months)
  var balloon = Math.min(nonNegative(balloonAmount), principal)
  var r = monthlyRateFromAnnual(annualRatePercent)
  var payment = isZeroRate(r)
    ? (principal - balloon) / months
    : (principal - balloon / Math.pow(1 + r, months)) * r / (1 - Math.pow(1 + r, -months))
  var balance = principal
  var schedule = []

  for (var i = 1; i <= months; i += 1) {
    var interest = balance * r
    var principalPart = payment - interest
    if (i === months) principalPart = balance
    var rowPayment = principalPart + interest
    balance = Math.max(0, balance - principalPart)
    schedule.push({
      month: i,
      payment: rowPayment,
      principal: principalPart,
      interest: interest,
      balance: balance
    })
  }

  var result = baseResult(principal, annualRatePercent, months, 'balloon', schedule)
  result.balloonAmount = balloon
  result.monthlyPayment = payment
  return result
}
```

（末期一把清偿剩余余额，数学上恰好 = 月供+尾款；`monthlyPayment` 显式覆盖，避免 months=1 时 baseResult 取到含尾款的首期值。）

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 尾款贷(气球贷)计算 calcBalloonLoan

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 4: calcRentToOwn 以租代购测算

**Files:**
- Modify: `utils/loan.js`（新函数插在 calcBalloonLoan 之后；exports 追加）
- Test: `tests/loan.test.js`

**Interfaces:**
- Consumes: Task 2 的 `inferMonthlyRateFromPayment(financed, rent, months, buyout)`。
- Produces: `calcRentToOwn(carPrice, downPayment, monthlyRent, months, buyout)` → `{ carPrice, downPayment, monthlyRent, months, buyout, totalCost, premiumOverCash, hasImpliedRate, impliedMonthlyRate, impliedAnnualNominalRate, impliedAnnualEffectiveRate }`（利率均为小数，如 0.015）。

- [ ] **Step 1: 写失败测试** — 追加：

```js
;(function () {
  const price = 200000, down = 20000, B = 50000, m = 36, r = 0.015
  const financed = price - down
  const rent = (financed - B / Math.pow(1 + r, m)) * r / (1 - Math.pow(1 + r, -m))
  const res = loan.calcRentToOwn(price, down, rent, m, B)
  closeTo(res.impliedMonthlyRate, r, 0.0000001)
  closeTo(res.totalCost, down + rent * m + B, 0.01)
  closeTo(res.premiumOverCash, res.totalCost - price, 0.000001)
  assert.strictEqual(res.hasImpliedRate, true)
  closeTo(res.impliedAnnualEffectiveRate, Math.pow(1 + r, 12) - 1, 0.000001)
})()
// 车价缺省(0)：不算隐含利率、溢价为0，总费用照算
;(function () {
  const res = loan.calcRentToOwn(0, 10000, 3000, 24, 20000)
  assert.strictEqual(res.hasImpliedRate, false)
  assert.strictEqual(res.impliedMonthlyRate, 0)
  assert.strictEqual(res.premiumOverCash, 0)
  closeTo(res.totalCost, 10000 + 3000 * 24 + 20000, 0.01)
})()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/loan.test.js` → `calcRentToOwn is not a function`。

- [ ] **Step 3: 实现**（exports 加 `calcRentToOwn: calcRentToOwn,`）：

```js
function calcRentToOwn(carPrice, downPayment, monthlyRent, months, buyout) {
  carPrice = nonNegative(carPrice)
  downPayment = nonNegative(downPayment)
  monthlyRent = nonNegative(monthlyRent)
  buyout = nonNegative(buyout)
  months = normalizeMonths(months)
  var financed = Math.max(0, carPrice - downPayment)
  var hasImpliedRate = financed > 0 && monthlyRent > 0
  var impliedMonthlyRate = hasImpliedRate ? inferMonthlyRateFromPayment(financed, monthlyRent, months, buyout) : 0
  var totalCost = downPayment + monthlyRent * months + buyout
  return {
    carPrice: carPrice,
    downPayment: downPayment,
    monthlyRent: monthlyRent,
    months: months,
    buyout: buyout,
    totalCost: totalCost,
    premiumOverCash: carPrice > 0 ? totalCost - carPrice : 0,
    hasImpliedRate: hasImpliedRate,
    impliedMonthlyRate: impliedMonthlyRate,
    impliedAnnualNominalRate: impliedMonthlyRate * 12,
    impliedAnnualEffectiveRate: Math.pow(1 + impliedMonthlyRate, 12) - 1
  }
}
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 以租代购方案测算 calcRentToOwn

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 5: calcRentPricing 定价反推

**Files:**
- Modify: `utils/loan.js`（新函数插在 calcRentToOwn 之后；exports 追加）
- Test: `tests/loan.test.js`

**Interfaces:**
- Consumes: Task 4 的 calcRentToOwn（测试闭环用）。
- Produces: `calcRentPricing(carPrice, downPayment, months, buyout, targetAnnualRatePercent)` → `{ carPrice, downPayment, months, buyout, targetAnnualRate（百分数原样）, monthlyRent, totalCost, premiumOverCash }`；月租夹到 ≥0。

- [ ] **Step 1: 写失败测试** — 追加：

```js
// 定价 ↔ 测算 互为逆运算
;(function () {
  const pricing = loan.calcRentPricing(200000, 20000, 36, 50000, 18)
  const back = loan.calcRentToOwn(200000, 20000, pricing.monthlyRent, 36, 50000)
  closeTo(back.impliedAnnualNominalRate, 0.18, 0.000001)
  closeTo(pricing.totalCost, 20000 + pricing.monthlyRent * 36 + 50000, 0.01)
})()
// 零利率：月租 = (融资额-尾款)/期数
closeTo(loan.calcRentPricing(120000, 20000, 20, 40000, 0).monthlyRent, 3000, 0.000001)
// 尾款已覆盖融资额 → 月租 0
assert.strictEqual(loan.calcRentPricing(100000, 20000, 12, 200000, 10).monthlyRent, 0)
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/loan.test.js` → `calcRentPricing is not a function`。

- [ ] **Step 3: 实现**（exports 加 `calcRentPricing: calcRentPricing,`）：

```js
function calcRentPricing(carPrice, downPayment, months, buyout, targetAnnualRatePercent) {
  carPrice = nonNegative(carPrice)
  downPayment = nonNegative(downPayment)
  buyout = nonNegative(buyout)
  months = normalizeMonths(months)
  var r = monthlyRateFromAnnual(targetAnnualRatePercent)
  var financed = Math.max(0, carPrice - downPayment)
  var rent = isZeroRate(r)
    ? (financed - buyout) / months
    : (financed - buyout / Math.pow(1 + r, months)) * r / (1 - Math.pow(1 + r, -months))
  rent = Math.max(0, rent)
  var totalCost = downPayment + rent * months + buyout
  return {
    carPrice: carPrice,
    downPayment: downPayment,
    months: months,
    buyout: buyout,
    targetAnnualRate: nonNegative(targetAnnualRatePercent),
    monthlyRent: rent,
    totalCost: totalCost,
    premiumOverCash: carPrice > 0 ? totalCost - carPrice : 0
  }
}
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 以租代购定价反推 calcRentPricing

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 6: calcActualRate 含前置费用

**Files:**
- Modify: `utils/loan.js:178-199`（calcActualRate）
- Test: `tests/loan.test.js`

**Interfaces:**
- Produces: `calcActualRate(principal, monthlyPayment, months, claimedMonthlyRatePercent, upfrontFee)` — 第 5 参 `upfrontFee`（元，缺省 0，夹到 ≤principal）。返回值在 V1 字段之上追加：`upfrontFee`、`feeAdjustedMonthlyRate`、`feeAdjustedAnnualNominalRate`、`feeAdjustedAnnualEffectiveRate`（含费口径 = 以净到手本金 principal−fee 反推）。`totalInterest` 保持名义本金口径不变。

- [ ] **Step 1: 写失败测试** — 追加：

```js
// fee=0：含费口径与名义口径一致（含 V1 兼容）
;(function () {
  const res = loan.calcActualRate(350000, 11816.5, 36, 0.59)
  assert.strictEqual(res.feeAdjustedMonthlyRate, res.monthlyRate)
  assert.strictEqual(res.upfrontFee, 0)
})()
// 收费 → 含费利率必然更高；且往返可还原
;(function () {
  const net = 340000, m = 36, r = 0.0125
  const pay = net * r * Math.pow(1 + r, m) / (Math.pow(1 + r, m) - 1)
  const res = loan.calcActualRate(350000, pay, m, 0, 10000)
  closeTo(res.feeAdjustedMonthlyRate, r, 0.0000001)
  assert.ok(res.feeAdjustedMonthlyRate > res.monthlyRate)
  closeTo(res.totalInterest, pay * m - 350000, 0.01)
})()
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/loan.test.js` → `feeAdjustedMonthlyRate` undefined 断言失败。

- [ ] **Step 3: 实现** — 替换 calcActualRate：

```js
function calcActualRate(principal, monthlyPayment, months, claimedMonthlyRatePercent, upfrontFee) {
  principal = nonNegative(principal)
  monthlyPayment = nonNegative(monthlyPayment)
  months = normalizeMonths(months)
  upfrontFee = Math.min(nonNegative(upfrontFee), principal)
  var monthlyRate = inferMonthlyRateFromPayment(principal, monthlyPayment, months)
  var feeAdjustedMonthlyRate = upfrontFee > 0
    ? inferMonthlyRateFromPayment(principal - upfrontFee, monthlyPayment, months)
    : monthlyRate
  var claimedMonthlyRate = nonNegative(claimedMonthlyRatePercent) / 100
  var totalPayment = monthlyPayment * months
  var totalInterest = Math.max(0, totalPayment - principal)
  return {
    principal: principal,
    months: months,
    monthlyPayment: monthlyPayment,
    monthlyRate: monthlyRate,
    annualNominalRate: monthlyRate * 12,
    annualEffectiveRate: Math.pow(1 + monthlyRate, 12) - 1,
    upfrontFee: upfrontFee,
    feeAdjustedMonthlyRate: feeAdjustedMonthlyRate,
    feeAdjustedAnnualNominalRate: feeAdjustedMonthlyRate * 12,
    feeAdjustedAnnualEffectiveRate: Math.pow(1 + feeAdjustedMonthlyRate, 12) - 1,
    totalPayment: totalPayment,
    totalInterest: totalInterest,
    claimedMonthlyRate: claimedMonthlyRate,
    claimedMultiple: claimedMonthlyRate > 0 ? monthlyRate / claimedMonthlyRate : 0,
    claimedMonthlyGap: monthlyRate - claimedMonthlyRate
  }
}
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 查真利率支持前置费用摊入(含费IRR)

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 7: calcPrepayment 违约金

**Files:**
- Modify: `utils/loan.js:289-341`（calcPrepayment）
- Test: `tests/loan.test.js`

**Interfaces:**
- Produces: `calcPrepayment(principal, annualRatePercent, months, paidMonths, prepayAmount, reduceMode, penaltyPercent, penaltyAmount)` — `penaltyAmount>0` 时用固定额，否则 `实际提前还款额×penaltyPercent/100`（实际额 = min(prepayAmount, 剩余本金)）。返回值追加 `penalty`、`netSaved = interestSaved − penalty`（**可为负，不夹 0**）。已还满分支 penalty=0、netSaved=0。

- [ ] **Step 1: 写失败测试** — 追加：

```js
// 比例违约金：3% × 50000 = 1500
;(function () {
  const res = loan.calcPrepayment(350000, 13.14, 36, 12, 50000, 'term', 3)
  closeTo(res.penalty, 1500, 0.000001)
  closeTo(res.netSaved, res.interestSaved - 1500, 0.000001)
})()
// 固定额优先于比例
closeTo(loan.calcPrepayment(350000, 13.14, 36, 12, 50000, 'term', 3, 2000).penalty, 2000, 0.000001)
// 违约金按实际还入的钱算（提前额超过剩余本金时）
;(function () {
  const res = loan.calcPrepayment(350000, 13.14, 36, 12, 9999999, 'term', 3)
  closeTo(res.penalty, res.remainingBalance * 0.03, 0.01)
})()
// 净省可为负：临近结清还大额+高违约金
assert.ok(loan.calcPrepayment(350000, 13.14, 36, 34, 20000, 'term', 10).netSaved < 0)
// 兼容：不传违约金参数 → penalty 0, netSaved = interestSaved
;(function () {
  const res = loan.calcPrepayment(350000, 13.14, 36, 12, 50000, 'term')
  assert.strictEqual(res.penalty, 0)
  closeTo(res.netSaved, res.interestSaved, 0.000001)
})()
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/loan.test.js` → penalty undefined。

- [ ] **Step 3: 实现** — calcPrepayment 改动三处：

签名行改为：

```js
function calcPrepayment(principal, annualRatePercent, months, paidMonths, prepayAmount, reduceMode, penaltyPercent, penaltyAmount) {
```

已还满的 early-return 对象里追加两行：

```js
      penalty: 0,
      netSaved: 0,
```

`afterPrepayBalance` 计算之后插入：

```js
  var effectivePrepay = Math.min(prepayAmount, remainingBalance)
  var penalty = nonNegative(penaltyAmount) > 0
    ? nonNegative(penaltyAmount)
    : effectivePrepay * nonNegative(penaltyPercent) / 100
```

末尾 return 对象在 `interestSaved` 一行之后追加：

```js
    penalty: penalty,
    netSaved: Math.max(0, oldRemainingInterest - newRemainingInterest) - penalty,
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add utils/loan.js tests/loan.test.js
git commit -m "feat: 提前还款违约金与净省金额

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 8: 页面万/元单位切换

**Files:**
- Modify: `pages/index/index.js`（helpers、data、setLoanType、build 5 处）
- Modify: `pages/index/index.wxml`（5 个面板的单位文案 + 切换控件）
- Test: `tests/page.test.js`

**Interfaces:**
- Produces: 页面 helper `amount(value, unit)`（`unit==='wan'` 时 ×10000）；表单字段 `unit: 'yuan'|'wan'`。大额字段（本金/车价/首付/尾款/提前还款额/商贷/公积金/费用/总利息）随 unit 缩放；**月供、月租、月供预算永远是元**。budgetForm 无 unit。
- 缺省值：paymentForm 'yuan'（setLoanType 切 home 时置 'wan'、切 car 置 'yuan'），comboForm/prepayForm 'wan'，actualForm/flatForm 'yuan'。

- [ ] **Step 1: 写失败测试** — `tests/page.test.js` 在 `const wxml = ...` 行之前插入：

```js
page.data.paymentForm = {
  principal: '35',
  months: '36',
  annualRate: '13.14',
  rateMode: 'annual',
  method: 'equalInstallment',
  unit: 'wan',
  inputMode: 'loan',
  carPrice: '',
  downRatio: '',
  downPayment: ''
}
const wanPayment = page.buildPaymentResult().primaryPayment
page.data.paymentForm.unit = 'yuan'
page.data.paymentForm.principal = '350000'
assert.strictEqual(page.buildPaymentResult().primaryPayment, wanPayment)

page.setLoanType({ currentTarget: { dataset: { value: 'home' } } })
assert.strictEqual(page.data.paymentForm.unit, 'wan')
page.setLoanType({ currentTarget: { dataset: { value: 'car' } } })
assert.strictEqual(page.data.paymentForm.unit, 'yuan')
```

（`inputMode/carPrice/downRatio/downPayment` 是 Task 9 的字段，这里一并写进 mock 表单避免 Task 9 再改这段测试；Task 8 实现只需容忍这些字段存在。）

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js` → 万/元结果不等或 unit undefined。

- [ ] **Step 3: 实现**

`index.js` 顶部 helper 区（`rateText` 之后）加：

```js
function amount(value, unit) {
  return loan.toNumber(value) * (unit === 'wan' ? 10000 : 1)
}

function unitLabel(unit) {
  return unit === 'wan' ? '万' : '元'
}
```

data 中：`paymentForm` 加 `unit: 'yuan'`；`comboForm` 加 `unit: 'wan'`；`prepayForm` 加 `unit: 'wan'`；`actualForm` 加 `unit: 'yuan'`；`flatForm` 加 `unit: 'yuan'`。

`setLoanType` 的 setData 对象加一行：

```js
      'paymentForm.unit': loanType === 'home' ? 'wan' : 'yuan',
```

build 方法换算（月供类字段不动）：
- `buildPaymentResult`：`const principal = amount(form.principal, form.unit)`，传给 `loan.calculateByMethod(principal, ...)`。
- `buildComboResult`：`amount(form.commercialPrincipal, form.unit)`、`amount(form.fundPrincipal, form.unit)` 传入 calcCompositeLoan；copyText 里两处 `money(loan.toNumber(...))` 同步换成 `money(amount(...))`。
- `buildActualResult`：`const principal = amount(form.principal, form.unit)` 传入。
- `buildFlatResult`：同上。
- `buildPrepayResult`：principal、prepayAmount 都过 `amount(form.xxx, form.unit)`。

`index.wxml`：5 个面板中每个大额字段的 `<text class="unit">元</text>` 改为 `<text class="unit">{{xxxForm.unit == 'wan' ? '万' : '元'}}</text>`（payment 的贷款本金、combo 的商贷/公积金本金、actual 的贷款本金、flat 的贷款本金、prepay 的贷款本金和提前还款）；每个面板在第一个金额字段后加：

```xml
    <view class="segmented two">
      <text class="{{paymentForm.unit == 'yuan' ? 'active' : ''}}" data-form="paymentForm" data-field="unit" data-value="yuan" bindtap="setFormValue">按元</text>
      <text class="{{paymentForm.unit == 'wan' ? 'active' : ''}}" data-form="paymentForm" data-field="unit" data-value="wan" bindtap="setFormValue">按万</text>
    </view>
```

（其余 4 个面板把 `paymentForm` 换成对应表单名。actual 面板的"实际月供"、budget 面板整个，单位保持"元"字面量。）

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 金额输入万/元单位切换(房贷默认万)

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 9: 算月供的车价−首付模式

**Files:**
- Modify: `pages/index/index.js`（paymentForm 字段、onInput/setFormValue 联动、buildPaymentResult、data 加 downRatioOptions）
- Modify: `pages/index/index.wxml`（payment 面板）
- Test: `tests/page.test.js`

**Interfaces:**
- Produces: `paymentForm` 新字段 `inputMode: 'loan'|'price'`、`carPrice`、`downRatio`（''=自定义额）、`downPayment`；price 模式贷款额 = carPrice − 首付（首付 = downRatio 存在时 carPrice×ratio/100，否则 downPayment）。`paymentResult` 新增 `loanAmount`（格式化贷款额）。
- 联动规则（onInput/setFormValue 通用逻辑）：输入 `downPayment` 清空 `downRatio`；点选 `downRatio` 清空 `downPayment`。

- [ ] **Step 1: 写失败测试** — `tests/page.test.js` 追加（Task 8 那段之后）：

```js
page.data.paymentForm = {
  principal: '',
  months: '36',
  annualRate: '13.14',
  rateMode: 'annual',
  method: 'equalInstallment',
  unit: 'yuan',
  inputMode: 'price',
  carPrice: '437000',
  downRatio: '20',
  downPayment: ''
}
const priceResult = page.buildPaymentResult()
assert.strictEqual(priceResult.loanAmount, '349,600.00')
assert.ok(priceResult.copyText.includes('车价'))

page.data.paymentForm.downRatio = ''
page.data.paymentForm.downPayment = '87000'
assert.strictEqual(page.buildPaymentResult().loanAmount, '350,000.00')

// 联动：点比例清自定义额，输自定义额清比例
page.setFormValue({ currentTarget: { dataset: { form: 'paymentForm', field: 'downRatio', value: '30' } } })
assert.strictEqual(page.data.paymentForm.downPayment, '')
page.onInput({ currentTarget: { dataset: { form: 'paymentForm', field: 'downPayment' } }, detail: { value: '50000' } })
assert.strictEqual(page.data.paymentForm.downRatio, '')
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js` → loanAmount undefined。

- [ ] **Step 3: 实现**

data：paymentForm 加 `inputMode: 'loan', carPrice: '', downRatio: '', downPayment: ''`；data 顶层加 `downRatioOptions: ['20', '30', '40', '50'],`。

onInput / setFormValue 加联动（两个方法整体替换）：

```js
  onInput(event) {
    const form = event.currentTarget.dataset.form
    const field = event.currentTarget.dataset.field
    const patch = { [`${form}.${field}`]: event.detail.value }
    if (field === 'downPayment') patch[`${form}.downRatio`] = ''
    if (field === 'balloonAmount') patch[`${form}.balloonRatio`] = ''
    this.setData(patch, () => this.recalculate())
  },

  setFormValue(event) {
    const form = event.currentTarget.dataset.form
    const field = event.currentTarget.dataset.field
    const value = event.currentTarget.dataset.value
    const patch = { [`${form}.${field}`]: value }
    if (field === 'downRatio') patch[`${form}.downPayment`] = ''
    if (field === 'balloonRatio') patch[`${form}.balloonAmount`] = ''
    this.setData(patch, () => this.recalculate())
  },
```

（balloon 两行是 Task 12 的联动，一次写好，对不存在的字段无副作用。）

buildPaymentResult 头部替换：

```js
  buildPaymentResult() {
    const form = this.data.paymentForm
    let principal = amount(form.principal, form.unit)
    let carPrice = 0
    let downPayment = 0
    if (form.inputMode === 'price') {
      carPrice = amount(form.carPrice, form.unit)
      downPayment = form.downRatio
        ? carPrice * loan.toNumber(form.downRatio) / 100
        : amount(form.downPayment, form.unit)
      principal = Math.max(0, carPrice - downPayment)
    }
    const annualRate = asAnnualRate(form.annualRate, form.rateMode)
    const result = loan.calculateByMethod(principal, annualRate, form.months, form.method)
```

copyText 数组在 `贷款本金` 行前面插（仅 price 模式）：

```js
    const priceLines = form.inputMode === 'price'
      ? [`车价：${money(carPrice)} 元`, `首付：${money(downPayment)} 元`]
      : []
```

并把 copyText 组装改为 `[...this.loanContextLines(), ...priceLines, ...]`。返回对象加 `loanAmount: money(result.principal),`。

WXML payment 面板：本金字段外包 `wx:if`，前面加模式切换，price 分支加车价/首付控件：

```xml
    <view class="segmented two">
      <text class="{{paymentForm.inputMode == 'loan' ? 'active' : ''}}" data-form="paymentForm" data-field="inputMode" data-value="loan" bindtap="setFormValue">填贷款额</text>
      <text class="{{paymentForm.inputMode == 'price' ? 'active' : ''}}" data-form="paymentForm" data-field="inputMode" data-value="price" bindtap="setFormValue">{{loanType == 'car' ? '车价−首付' : '房价−首付'}}</text>
    </view>
    <view class="field" wx:if="{{paymentForm.inputMode == 'loan'}}">
      <text class="label">贷款本金</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="输入贷款金额" type="digit" value="{{paymentForm.principal}}" data-form="paymentForm" data-field="principal" bindinput="onInput" />
      <text class="unit">{{paymentForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <block wx:if="{{paymentForm.inputMode == 'price'}}">
      <view class="field">
        <text class="label">{{loanType == 'car' ? '车价' : '房价'}}</text>
        <input class="input" placeholder-class="input-placeholder" placeholder="落地总价" type="digit" value="{{paymentForm.carPrice}}" data-form="paymentForm" data-field="carPrice" bindinput="onInput" />
        <text class="unit">{{paymentForm.unit == 'wan' ? '万' : '元'}}</text>
      </view>
      <view class="quick-row">
        <text wx:for="{{downRatioOptions}}" wx:key="*this" data-form="paymentForm" data-field="downRatio" data-value="{{item}}" bindtap="setFormValue">首付{{item}}%</text>
      </view>
      <view class="field">
        <text class="label">首付{{paymentForm.downRatio ? paymentForm.downRatio + '%' : '金额'}}</text>
        <input class="input" placeholder-class="input-placeholder" placeholder="自定义首付金额" type="digit" value="{{paymentForm.downPayment}}" data-form="paymentForm" data-field="downPayment" bindinput="onInput" />
        <text class="unit">{{paymentForm.unit == 'wan' ? '万' : '元'}}</text>
      </view>
    </block>
```

result-grid 加一格（price 模式显示）：

```xml
      <view class="metric" wx:if="{{paymentForm.inputMode == 'price'}}"><text>贷款额</text><text>{{paymentResult.loanAmount}}</text></view>
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 算月供支持车价/房价−首付输入模式

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 10: 查真利率页面（前置费用 + 只知总利息）

**Files:**
- Modify: `pages/index/index.js`（actualForm 字段、buildActualResult）
- Modify: `pages/index/index.wxml`（actual 面板）
- Test: `tests/page.test.js`

**Interfaces:**
- Consumes: Task 6 的 `calcActualRate(..., upfrontFee)`。
- Produces: `actualForm` 新字段 `upfrontFee: ''`、`inputMode: 'payment'|'interest'`、`totalInterest: ''`；interest 模式折算月供 = (本金+总利息)/期数。`actualResult` 新增 `feeAdjustedMonthlyRate`、`feeAdjustedAnnualEffectiveRate`（格式化）。

- [ ] **Step 1: 写失败测试** — 追加：

```js
page.data.actualForm = {
  principal: '350000',
  months: '36',
  monthlyPayment: '',
  totalInterest: '75394',
  inputMode: 'interest',
  upfrontFee: '0',
  claimedMonthlyRate: '0.59',
  claimedRateMode: 'monthly',
  unit: 'yuan'
}
const interestModeResult = page.buildActualResult()
assert.strictEqual(interestModeResult.monthlyRate, '1.0950%')

page.data.actualForm.inputMode = 'payment'
page.data.actualForm.monthlyPayment = '11816.50'
page.data.actualForm.upfrontFee = '10000'
const feeResult = page.buildActualResult()
assert.ok(parseFloat(feeResult.feeAdjustedMonthlyRate) > parseFloat(feeResult.monthlyRate))
assert.ok(feeResult.copyText.includes('含费'))
```

（(350000+75394)/36 = 11816.5 → 反推月利率 ≈0.0109497，formatPercent(…,4) 后为 `1.0950%`。）

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js`。

- [ ] **Step 3: 实现**

data：actualForm 加 `upfrontFee: '', inputMode: 'payment', totalInterest: ''`（unit 已在 Task 8 加）。

buildActualResult 头部替换：

```js
  buildActualResult() {
    const form = this.data.actualForm
    const principal = amount(form.principal, form.unit)
    const upfrontFee = amount(form.upfrontFee, form.unit)
    const months = Math.max(1, Math.round(loan.toNumber(form.months)))
    const monthlyPayment = form.inputMode === 'interest'
      ? (principal + amount(form.totalInterest, form.unit)) / months
      : loan.toNumber(form.monthlyPayment)
    const claimedMonthlyRate = asMonthlyRatePercent(form.claimedMonthlyRate, form.claimedRateMode)
    const result = loan.calcActualRate(principal, monthlyPayment, months, claimedMonthlyRate, upfrontFee)
```

copyText 在"真实月利率"行后加两行：

```js
      `含费月利率：${percent(result.feeAdjustedMonthlyRate, 4)}`,
      `含费复利年化：${percent(result.feeAdjustedAnnualEffectiveRate, 2)}`,
```

interest 模式时"月供"行改为 `` `折算月供：${money(result.monthlyPayment)} 元` ``（用三元切换文案）。返回对象加：

```js
      feeAdjustedMonthlyRate: percent(result.feeAdjustedMonthlyRate, 4),
      feeAdjustedAnnualEffectiveRate: percent(result.feeAdjustedAnnualEffectiveRate, 2),
```

WXML actual 面板：月供字段外包 `wx:if="{{actualForm.inputMode == 'payment'}}"`，加模式切换和总利息字段：

```xml
    <view class="segmented two">
      <text class="{{actualForm.inputMode == 'payment' ? 'active' : ''}}" data-form="actualForm" data-field="inputMode" data-value="payment" bindtap="setFormValue">知道月供</text>
      <text class="{{actualForm.inputMode == 'interest' ? 'active' : ''}}" data-form="actualForm" data-field="inputMode" data-value="interest" bindtap="setFormValue">只知总利息</text>
    </view>
    <view class="field" wx:if="{{actualForm.inputMode == 'interest'}}">
      <text class="label">总利息</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="整个贷款多付的利息" type="digit" value="{{actualForm.totalInterest}}" data-form="actualForm" data-field="totalInterest" bindinput="onInput" />
      <text class="unit">{{actualForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="field">
      <text class="label">前置费用</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="GPS费/服务费等，没有留空" type="digit" value="{{actualForm.upfrontFee}}" data-form="actualForm" data-field="upfrontFee" bindinput="onInput" />
      <text class="unit">{{actualForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
```

result-grid 加两格：

```xml
      <view class="metric"><text>含费月利率</text><text>{{actualResult.feeAdjustedMonthlyRate}}</text></view>
      <view class="metric"><text>含费复利年化</text><text>{{actualResult.feeAdjustedAnnualEffectiveRate}}</text></view>
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 查真利率支持前置费用与总利息反推

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 11: 提前还款违约金页面

**Files:**
- Modify: `pages/index/index.js`（prepayForm 字段、buildPrepayResult）
- Modify: `pages/index/index.wxml`（prepay 面板）
- Modify: `pages/index/index.wxss`（`.negative` 类）
- Test: `tests/page.test.js`

**Interfaces:**
- Consumes: Task 7 的 `calcPrepayment(..., penaltyPercent, penaltyAmount)`。
- Produces: `prepayForm` 新字段 `penaltyMode: 'percent'|'amount'`、`penaltyPercent: ''`、`penaltyAmount: ''`；`prepayResult` 新增 `penalty`、`netSaved`（格式化）、`netSavedNegative`（boolean）。

- [ ] **Step 1: 写失败测试** — 追加：

```js
page.data.prepayForm = {
  principal: '350000',
  months: '36',
  annualRate: '13.14',
  rateMode: 'annual',
  paidMonths: '12',
  prepayAmount: '50000',
  reduceMode: 'term',
  penaltyMode: 'percent',
  penaltyPercent: '3',
  penaltyAmount: '',
  unit: 'yuan'
}
const prepayRes = page.buildPrepayResult()
assert.strictEqual(prepayRes.penalty, '1,500.00')
assert.strictEqual(prepayRes.netSavedNegative, false)
assert.ok(prepayRes.copyText.includes('净省'))

page.data.prepayForm.paidMonths = '34'
page.data.prepayForm.prepayAmount = '20000'
page.data.prepayForm.penaltyPercent = '10'
assert.strictEqual(page.buildPrepayResult().netSavedNegative, true)
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js`。

- [ ] **Step 3: 实现**

data：prepayForm 加 `penaltyMode: 'percent', penaltyPercent: '', penaltyAmount: ''`。

buildPrepayResult 的 calcPrepayment 调用替换：

```js
    const result = loan.calcPrepayment(
      amount(form.principal, form.unit),
      annualRate,
      form.months,
      form.paidMonths,
      amount(form.prepayAmount, form.unit),
      form.reduceMode,
      form.penaltyMode === 'percent' ? loan.toNumber(form.penaltyPercent) : 0,
      form.penaltyMode === 'amount' ? amount(form.penaltyAmount, form.unit) : 0
    )
```

copyText"节省利息"行后加：

```js
      `违约金：${money(result.penalty)} 元`,
      `净省(扣违约金)：${money(result.netSaved)} 元`,
```

返回对象加：

```js
      penalty: money(result.penalty),
      netSaved: money(result.netSaved),
      netSavedNegative: result.netSaved < 0,
```

WXML prepay 面板"提前还款"字段后加：

```xml
    <view class="segmented two">
      <text class="{{prepayForm.penaltyMode == 'percent' ? 'active' : ''}}" data-form="prepayForm" data-field="penaltyMode" data-value="percent" bindtap="setFormValue">违约金%</text>
      <text class="{{prepayForm.penaltyMode == 'amount' ? 'active' : ''}}" data-form="prepayForm" data-field="penaltyMode" data-value="amount" bindtap="setFormValue">违约金额</text>
    </view>
    <view class="field" wx:if="{{prepayForm.penaltyMode == 'percent'}}">
      <text class="label">违约金比例</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="按提前还款额收，常见2~3，没有留空" type="digit" value="{{prepayForm.penaltyPercent}}" data-form="prepayForm" data-field="penaltyPercent" bindinput="onInput" />
      <text class="unit">%</text>
    </view>
    <view class="field" wx:if="{{prepayForm.penaltyMode == 'amount'}}">
      <text class="label">违约金额</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="固定违约金" type="digit" value="{{prepayForm.penaltyAmount}}" data-form="prepayForm" data-field="penaltyAmount" bindinput="onInput" />
      <text class="unit">{{prepayForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
```

result-grid：原"预计省利息"的 `metric main` 降为普通 `metric`，追加：

```xml
      <view class="metric"><text>违约金</text><text>{{prepayResult.penalty}}</text></view>
      <view class="metric main"><text>净省(扣违约金)</text><text class="{{prepayResult.netSavedNegative ? 'negative' : ''}}">{{prepayResult.netSaved}}</text></view>
```

`index.wxss` 末尾加：

```css
.metric text.negative {
  color: #a8442e;
}
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml pages/index/index.wxss tests/page.test.js
git commit -m "feat: 提前还款违约金输入与净省展示

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 12: 尾款贷 Tab

**Files:**
- Modify: `pages/index/index.js`（TOOL_OPTIONS.car、balloonForm、buildBalloonResult、recalculate、copySummary、data 加 balloonRatioOptions）
- Modify: `pages/index/index.wxml`（新面板）
- Test: `tests/page.test.js`

**Interfaces:**
- Consumes: Task 3 `calcBalloonLoan`、Task 8 `amount`、Task 9 的 ratio/amount 联动（onInput/setFormValue 已含 balloon 两行）。
- Produces: 工具 `balloon`（label 尾款贷）；`balloonForm { principal, months, annualRate, rateMode: 'annual', balloonRatio, balloonAmount, unit: 'yuan' }`；`balloonResult { monthlyPayment, balloonAmount, lastPayment, totalInterest, totalPayment, normalMonthly, normalInterest, schedulePreview, copyText }`。

- [ ] **Step 1: 写失败测试** — 把 page.test.js 中车贷 toolList 断言改为：

```js
assert.deepStrictEqual(page.data.toolList.map((item) => item.label), ['算月供', '查真利率', '平息换算', '尾款贷'])
```

并追加：

```js
page.data.balloonForm = {
  principal: '200000',
  months: '36',
  annualRate: '14.4',
  rateMode: 'annual',
  balloonRatio: '40',
  balloonAmount: '',
  unit: 'yuan'
}
const balloonRes = page.buildBalloonResult()
assert.ok(balloonRes.copyText.includes('尾款：80,000.00 元'))
assert.ok(parseFloat(balloonRes.monthlyPayment.replace(/,/g, '')) < parseFloat(balloonRes.normalMonthly.replace(/,/g, '')))
assert.ok(balloonRes.schedulePreview.length === 36)
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js`。

- [ ] **Step 3: 实现**

TOOL_OPTIONS.car 追加 `{ value: 'balloon', label: '尾款贷' }`。data 顶层加 `balloonRatioOptions: ['30', '40', '50'],`、`balloonResult: {},`，balloonForm 如接口所述。

recalculate()：加 `const balloonResult = this.buildBalloonResult()`，schedules 映射加 `balloon: balloonResult.schedulePreview`，setData 加 `balloonResult`。copySummary 的 summaries 加 `balloon: this.data.balloonResult.copyText`。

buildBalloonResult（放在 buildFlatResult 之后）：

```js
  buildBalloonResult() {
    const form = this.data.balloonForm
    const principal = amount(form.principal, form.unit)
    const balloonAmount = form.balloonRatio
      ? principal * loan.toNumber(form.balloonRatio) / 100
      : amount(form.balloonAmount, form.unit)
    const annualRate = asAnnualRate(form.annualRate, form.rateMode)
    const result = loan.calcBalloonLoan(principal, annualRate, form.months, balloonAmount)
    const normal = loan.calcEqualInstallment(principal, annualRate, form.months)
    const copyText = [
      ...this.loanContextLines(),
      `贷款本金：${money(result.principal)} 元`,
      `尾款：${money(result.balloonAmount)} 元`,
      rateText(form.annualRate, form.rateMode),
      `月供：${money(result.monthlyPayment)} 元`,
      `末期还款(含尾款)：${money(result.lastPayment)} 元`,
      `总利息：${money(result.totalInterest)} 元`,
      `对比等额本息：月供 ${money(normal.monthlyPayment)} 元 / 总利息 ${money(normal.totalInterest)} 元`
    ].join('\n')

    return {
      monthlyPayment: money(result.monthlyPayment),
      balloonAmount: money(result.balloonAmount),
      lastPayment: money(result.lastPayment),
      totalInterest: money(result.totalInterest),
      totalPayment: money(result.totalPayment),
      normalMonthly: money(normal.monthlyPayment),
      normalInterest: money(normal.totalInterest),
      schedulePreview: schedulePreview(result.schedule),
      copyText
    }
  },
```

WXML 在 flat 面板之后加：

```xml
  <view wx:if="{{activeTool == 'balloon'}}" class="panel">
    <view class="field">
      <text class="label">贷款本金</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="输入贷款金额" type="digit" value="{{balloonForm.principal}}" data-form="balloonForm" data-field="principal" bindinput="onInput" />
      <text class="unit">{{balloonForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="segmented two">
      <text class="{{balloonForm.unit == 'yuan' ? 'active' : ''}}" data-form="balloonForm" data-field="unit" data-value="yuan" bindtap="setFormValue">按元</text>
      <text class="{{balloonForm.unit == 'wan' ? 'active' : ''}}" data-form="balloonForm" data-field="unit" data-value="wan" bindtap="setFormValue">按万</text>
    </view>
    <view class="field">
      <text class="label">还款期数</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="输入还款期数" type="number" value="{{balloonForm.months}}" data-form="balloonForm" data-field="months" bindinput="onInput" />
      <text class="unit">期</text>
    </view>
    <view class="quick-row">
      <text wx:for="{{termOptionList}}" wx:key="value" data-form="balloonForm" data-value="{{item.value}}" bindtap="setMonths">{{item.label}}</text>
    </view>
    <view class="field">
      <text class="label">{{balloonForm.rateMode == 'monthly' ? '月息%' : '年化%'}}</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="{{balloonForm.rateMode == 'monthly' ? '例如 0.59' : '例如 13.14'}}" type="digit" value="{{balloonForm.annualRate}}" data-form="balloonForm" data-field="annualRate" bindinput="onInput" />
      <text class="unit">%</text>
    </view>
    <view class="segmented two">
      <text class="{{balloonForm.rateMode == 'annual' ? 'active' : ''}}" data-form="balloonForm" data-field="rateMode" data-value="annual" bindtap="setFormValue">年化%</text>
      <text class="{{balloonForm.rateMode == 'monthly' ? 'active' : ''}}" data-form="balloonForm" data-field="rateMode" data-value="monthly" bindtap="setFormValue">月息%</text>
    </view>
    <view class="quick-row">
      <text wx:for="{{balloonRatioOptions}}" wx:key="*this" data-form="balloonForm" data-field="balloonRatio" data-value="{{item}}" bindtap="setFormValue">尾款{{item}}%</text>
    </view>
    <view class="field">
      <text class="label">尾款{{balloonForm.balloonRatio ? balloonForm.balloonRatio + '%' : '金额'}}</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="自定义尾款金额" type="digit" value="{{balloonForm.balloonAmount}}" data-form="balloonForm" data-field="balloonAmount" bindinput="onInput" />
      <text class="unit">{{balloonForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="result-grid">
      <view class="metric main"><text>月供</text><text>{{balloonResult.monthlyPayment}}</text></view>
      <view class="metric"><text>末期还款(含尾款)</text><text>{{balloonResult.lastPayment}}</text></view>
      <view class="metric"><text>总利息</text><text>{{balloonResult.totalInterest}}</text></view>
      <view class="metric"><text>还款总额</text><text>{{balloonResult.totalPayment}}</text></view>
      <view class="metric"><text>等额本息月供</text><text>{{balloonResult.normalMonthly}}</text></view>
      <view class="metric"><text>等额本息总利息</text><text>{{balloonResult.normalInterest}}</text></view>
    </view>
  </view>
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 新增尾款贷工具Tab

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 13: 租购 Tab（测算 + 定价反推）

**Files:**
- Modify: `pages/index/index.js`（TOOL_OPTIONS.car、rtoForm、buildRtoResult、recalculate、copySummary）
- Modify: `pages/index/index.wxml`（新面板）
- Test: `tests/page.test.js`

**Interfaces:**
- Consumes: Task 4 `calcRentToOwn`、Task 5 `calcRentPricing`、Task 8 `amount`。
- Produces: 工具 `rto`（label 租购）；`rtoForm { mode: 'analyze'|'pricing', carPrice, downPayment, monthlyRent, months, buyout, bankAnnualRate, targetAnnualRate, unit: 'yuan' }`；`rtoResult`（analyze: totalCost/premiumOverCash/impliedMonthlyRate/impliedAnnualNominalRate/impliedAnnualEffectiveRate/bankCompare/copyText；pricing: monthlyRent/totalCost/premiumOverCash/copyText）。无还款明细。
- **约束：pricing 模式 copyText 不含"目标"二字。**

- [ ] **Step 1: 写失败测试** — 车贷 toolList 断言改为：

```js
assert.deepStrictEqual(page.data.toolList.map((item) => item.label), ['算月供', '查真利率', '平息换算', '尾款贷', '租购'])
```

并追加：

```js
page.data.rtoForm = {
  mode: 'analyze',
  carPrice: '200000',
  downPayment: '20000',
  monthlyRent: '6200',
  months: '36',
  buyout: '50000',
  bankAnnualRate: '7.08',
  targetAnnualRate: '',
  unit: 'yuan'
}
const rtoRes = page.buildRtoResult()
assert.strictEqual(rtoRes.totalCost, '293,200.00')
assert.ok(rtoRes.copyText.includes('隐含'))
assert.ok(rtoRes.bankCompare !== '')

page.data.rtoForm.mode = 'pricing'
page.data.rtoForm.targetAnnualRate = '18'
const pricingRes = page.buildRtoResult()
assert.ok(parseFloat(pricingRes.monthlyRent.replace(/,/g, '')) > 0)
assert.ok(!pricingRes.copyText.includes('目标'))
assert.ok(!pricingRes.copyText.includes('收益'))
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js`。

- [ ] **Step 3: 实现**

TOOL_OPTIONS.car 追加 `{ value: 'rto', label: '租购' }`。data 加 `rtoResult: {},` 与 rtoForm（接口所述字段，mode: 'analyze'）。

recalculate()：加 `const rtoResult = this.buildRtoResult()`，setData 带上（schedules 映射不加 rto）。copySummary 加 `rto: this.data.rtoResult.copyText`。

buildRtoResult（放在 buildBalloonResult 之后）：

```js
  buildRtoResult() {
    const form = this.data.rtoForm
    const carPrice = amount(form.carPrice, form.unit)
    const downPayment = amount(form.downPayment, form.unit)
    const buyout = amount(form.buyout, form.unit)
    const months = Math.max(1, Math.round(loan.toNumber(form.months)))

    if (form.mode === 'pricing') {
      const pricing = loan.calcRentPricing(carPrice, downPayment, months, buyout, loan.toNumber(form.targetAnnualRate))
      const copyText = [
        ...this.loanContextLines(),
        '以租代购方案',
        `首付/保证金：${money(downPayment)} 元`,
        `月租：${money(pricing.monthlyRent)} 元 × ${months} 期`,
        `期满尾款：${money(buyout)} 元`,
        `总费用：${money(pricing.totalCost)} 元`
      ].join('\n')
      return {
        mode: 'pricing',
        monthlyRent: money(pricing.monthlyRent),
        totalCost: money(pricing.totalCost),
        premiumOverCash: money(pricing.premiumOverCash),
        copyText
      }
    }

    const rto = loan.calcRentToOwn(carPrice, downPayment, loan.toNumber(form.monthlyRent), months, buyout)
    const bankRate = loan.toNumber(form.bankAnnualRate)
    let bankCompare = ''
    if (bankRate > 0 && carPrice > downPayment) {
      const bank = loan.calcEqualInstallment(carPrice - downPayment, bankRate, months)
      bankCompare = money(rto.totalCost - (bank.totalPayment + downPayment))
    }
    const copyText = [
      ...this.loanContextLines(),
      '以租代购测算',
      `首付/保证金：${money(downPayment)} 元，月租 ${money(rto.monthlyRent)} 元 × ${months} 期，尾款 ${money(buyout)} 元`,
      `总费用：${money(rto.totalCost)} 元`,
      carPrice > 0 ? `比一次性买车多花：${money(rto.premiumOverCash)} 元` : '',
      rto.hasImpliedRate ? `隐含月利率：${percent(rto.impliedMonthlyRate, 4)}，隐含复利年化：${percent(rto.impliedAnnualEffectiveRate, 2)}` : '',
      bankCompare ? `比银行车贷多花：${bankCompare} 元` : ''
    ].filter(Boolean).join('\n')

    return {
      mode: 'analyze',
      totalCost: money(rto.totalCost),
      premiumOverCash: money(rto.premiumOverCash),
      impliedMonthlyRate: percent(rto.impliedMonthlyRate, 4),
      impliedAnnualNominalRate: percent(rto.impliedAnnualNominalRate, 2),
      impliedAnnualEffectiveRate: percent(rto.impliedAnnualEffectiveRate, 2),
      bankCompare,
      copyText
    }
  }
```

WXML 在 balloon 面板之后加：

```xml
  <view wx:if="{{activeTool == 'rto'}}" class="panel">
    <view class="segmented two">
      <text class="{{rtoForm.mode == 'analyze' ? 'active' : ''}}" data-form="rtoForm" data-field="mode" data-value="analyze" bindtap="setFormValue">方案测算</text>
      <text class="{{rtoForm.mode == 'pricing' ? 'active' : ''}}" data-form="rtoForm" data-field="mode" data-value="pricing" bindtap="setFormValue">定价反推</text>
    </view>
    <view class="field">
      <text class="label">车价{{rtoForm.mode == 'analyze' ? '(选填)' : ''}}</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="{{rtoForm.mode == 'analyze' ? '填了才算隐含年化' : '车辆成本价'}}" type="digit" value="{{rtoForm.carPrice}}" data-form="rtoForm" data-field="carPrice" bindinput="onInput" />
      <text class="unit">{{rtoForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="field">
      <text class="label">首付/保证金</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="没有填0或留空" type="digit" value="{{rtoForm.downPayment}}" data-form="rtoForm" data-field="downPayment" bindinput="onInput" />
      <text class="unit">{{rtoForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="field" wx:if="{{rtoForm.mode == 'analyze'}}">
      <text class="label">月租金</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="每月租金" type="digit" value="{{rtoForm.monthlyRent}}" data-form="rtoForm" data-field="monthlyRent" bindinput="onInput" />
      <text class="unit">元</text>
    </view>
    <view class="field">
      <text class="label">租期</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="多少期" type="number" value="{{rtoForm.months}}" data-form="rtoForm" data-field="months" bindinput="onInput" />
      <text class="unit">期</text>
    </view>
    <view class="quick-row">
      <text wx:for="{{termOptionList}}" wx:key="value" data-form="rtoForm" data-value="{{item.value}}" bindtap="setMonths">{{item.label}}</text>
    </view>
    <view class="field">
      <text class="label">期满尾款</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="期满过户尾款，没有填0" type="digit" value="{{rtoForm.buyout}}" data-form="rtoForm" data-field="buyout" bindinput="onInput" />
      <text class="unit">{{rtoForm.unit == 'wan' ? '万' : '元'}}</text>
    </view>
    <view class="field" wx:if="{{rtoForm.mode == 'analyze'}}">
      <text class="label">银行年化(选填)</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="填了对比银行车贷" type="digit" value="{{rtoForm.bankAnnualRate}}" data-form="rtoForm" data-field="bankAnnualRate" bindinput="onInput" />
      <text class="unit">%</text>
    </view>
    <view class="field" wx:if="{{rtoForm.mode == 'pricing'}}">
      <text class="label">目标年化</text>
      <input class="input" placeholder-class="input-placeholder" placeholder="想要的年化收益" type="digit" value="{{rtoForm.targetAnnualRate}}" data-form="rtoForm" data-field="targetAnnualRate" bindinput="onInput" />
      <text class="unit">%</text>
    </view>
    <view class="result-grid" wx:if="{{rtoResult.mode == 'analyze'}}">
      <view class="metric main"><text>总费用</text><text>{{rtoResult.totalCost}}</text></view>
      <view class="metric"><text>比全款多花</text><text>{{rtoResult.premiumOverCash}}</text></view>
      <view class="metric"><text>隐含月利率</text><text>{{rtoResult.impliedMonthlyRate}}</text></view>
      <view class="metric"><text>隐含复利年化</text><text>{{rtoResult.impliedAnnualEffectiveRate}}</text></view>
      <view class="metric" wx:if="{{rtoResult.bankCompare}}"><text>比银行车贷多花</text><text>{{rtoResult.bankCompare}}</text></view>
    </view>
    <view class="result-grid" wx:if="{{rtoResult.mode == 'pricing'}}">
      <view class="metric main"><text>月租定价</text><text>{{rtoResult.monthlyRent}}</text></view>
      <view class="metric"><text>客户总费用</text><text>{{rtoResult.totalCost}}</text></view>
      <view class="metric"><text>比全款多花</text><text>{{rtoResult.premiumOverCash}}</text></view>
    </view>
  </view>
```

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 新增以租代购工具Tab(测算+定价反推)

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 14: 还款日历（首期年月）

**Files:**
- Modify: `pages/index/index.js`（schedulePreview 加参、monthLabel helper、setScheduleStart、5 处调用点）
- Modify: `pages/index/index.wxml`（schedule 区加 picker，期数列改 label）
- Test: `tests/page.test.js`

**Interfaces:**
- Produces: data `scheduleStartYm: ''`（'YYYY-MM'）；`schedulePreview(schedule, startYm)` 每行加 `label` 字段（未设起始月时 label=期数数字，设了则为 'YYYY-MM'，跨年正确）；`setScheduleStart(event)` 处理 picker change。`month` 字段保留作 wx:key。

- [ ] **Step 1: 写失败测试** — 追加：

```js
page.data.scheduleStartYm = '2026-11'
page.data.paymentForm = {
  principal: '120000', months: '4', annualRate: '0', rateMode: 'annual',
  method: 'equalInstallment', unit: 'yuan',
  inputMode: 'loan', carPrice: '', downRatio: '', downPayment: ''
}
const calRows = page.buildPaymentResult().schedulePreview
assert.deepStrictEqual(calRows.map((row) => row.label), ['2026-11', '2026-12', '2027-01', '2027-02'])
page.data.scheduleStartYm = ''
assert.strictEqual(page.buildPaymentResult().schedulePreview[0].label, '1')
```

- [ ] **Step 2: 跑测试确认失败** — Run: `node tests/page.test.js` → label undefined。

- [ ] **Step 3: 实现**

helper 区加：

```js
function monthLabel(startYm, monthIndex) {
  if (!startYm) return String(monthIndex)
  const parts = startYm.split('-')
  const total = Number(parts[0]) * 12 + (Number(parts[1]) - 1) + (monthIndex - 1)
  const y = Math.floor(total / 12)
  const m = (total % 12) + 1
  return y + '-' + (m < 10 ? '0' + m : m)
}
```

schedulePreview 改签名并加 label：

```js
function schedulePreview(schedule, startYm) {
  return schedule.filter(function (row) {
    return row.payment || row.principal || row.interest || row.balance
  }).map(function (row) {
    return {
      month: row.month,
      label: monthLabel(startYm, row.month),
      payment: money(row.payment),
      principal: money(row.principal),
      interest: money(row.interest),
      balance: money(row.balance)
    }
  })
}
```

5 处调用点（buildPaymentResult/buildComboResult/buildBudgetResult/buildPrepayResult/buildBalloonResult）改为 `schedulePreview(result.schedule, this.data.scheduleStartYm)`（prepay 传 `result.schedule` 同名）。

data 加 `scheduleStartYm: '',`。Page 方法加：

```js
  setScheduleStart(event) {
    this.setData({ scheduleStartYm: event.detail.value }, () => this.recalculate())
  },
```

WXML schedule-head 替换为：

```xml
    <view class="schedule-head">
      <text>还款明细</text>
      <picker mode="date" fields="month" value="{{scheduleStartYm}}" bindchange="setScheduleStart">
        <text>{{scheduleStartYm ? '首期 ' + scheduleStartYm : '设首期年月'}}</text>
      </picker>
      <text>共 {{activeSchedulePreview.length}} 期</text>
    </view>
```

table-row 的期数列 `{{item.month}}` 改 `{{item.label}}`（wx:key 保持 month）。

- [ ] **Step 4: 跑测试确认通过** — Run: `npm test` → 全绿。

- [ ] **Step 5: 提交**

```bash
git add pages/index/index.js pages/index/index.wxml tests/page.test.js
git commit -m "feat: 还款明细支持首期年月日历显示

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```

---

### Task 15: 文档同步 + 全量验证

**Files:**
- Modify: `docs/开发文档.md`、`README.md`
- Test: 全量

**Interfaces:**
- Consumes: 全部前置任务。

- [ ] **Step 1: 更新文档** — `docs/开发文档.md` 与 `README.md`：车贷工具列表加"尾款贷、租购"；功能规格加 8 项新能力（含费真利率、总利息反推、车价首付、违约金、万/元切换、还款日历、租购双模式）及各自输入输出；引擎 API 清单加 calcBalloonLoan/calcRentToOwn/calcRentPricing 及 calcActualRate/calcPrepayment/inferMonthlyRateFromPayment 的新参数。只改受影响章节。

- [ ] **Step 2: 全量测试**

Run: `npm test`
Expected: `loan calculator checks passed` + `page checks passed`。

- [ ] **Step 3: 独立验算脚本复跑** — 把本会话 scratchpad 的 `verify_loan.js` 复跑一遍确认 V1 的 22 项不变量零回归（脚本只调导出 API，不需改动即可跑）。

Run: `node /private/tmp/claude-501/-Users-yunxin-Desktop-----------/81119f86-8123-490e-bd89-5dedd6a54e61/scratchpad/verify_loan.js`
Expected: 22 项全 ok。

- [ ] **Step 4: 微信开发者工具手测清单**（人工/浏览器自动化）：
  - 车贷 5 个 Tab、房贷 4 个 Tab 全部切换无报错，空表单全 0 不崩；
  - 车价−首付模式：437000 车价 + 首付20% → 贷款额 349,600；
  - 尾款贷：20万/36期/14.4%/尾款40% 月供 < 等额本息月供；
  - 租购测算填银行年化出对比行；定价反推复制文案无"目标"字样；
  - 万/元切换：35万 = 350000 结果一致；
  - 违约金净省为负显示红色；
  - 首期年月 2026-11 + 4期 → 明细显示到 2027-02。

- [ ] **Step 5: 提交**

```bash
git add docs/ README.md
git commit -m "docs: V2 功能文档同步

Claude-Session: https://claude.ai/code/session_01NV6wKCiwoPU3CQ4mM82uAh"
```
